#function to display letters used so far

def display_letters(guess,secret_word, results):

    # add letters used to a string 
    letters = ''
    for x in guess:
        if x not in letters:
            letters += x
    print(letters)
    # pirnt results
    
    print(results)
